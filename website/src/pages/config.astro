---
import Layout from '../layouts/Layout.astro';
import Nav from '../components/Nav.astro';
import Footer from '../components/Footer.astro';
import ConfigExample from '../components/ConfigExample.astro';
import { Code } from 'astro:components';
---

<Layout title="Configuration -- mailypoppins" description="Complete configuration reference for mailypoppins email CLI.">
  <Nav />
  <main id="main-content" class="content page">
    <header class="page-header">
      <h1>Configuration</h1>
      <p class="subtitle">Complete reference for <code>~/.config/mailypoppins/config.toml</code></p>
    </header>

    <!-- Config File Location -->
    <section>
      <h2>Config File Location</h2>
      <p>
        All configuration lives in a single file: <code>~/.config/mailypoppins/config.toml</code>.
        Create it interactively with <code>mp config init</code>, or write it by hand.
        View the resolved config at any time with <code>mp config show</code>.
        Print the file path with <code>mp config path</code>.
      </p>
    </section>

    <!-- Full Example -->
    <section>
      <h2>Full Example</h2>
      <p>A complete single-account configuration with comments explaining each section.</p>
      <ConfigExample code={`theme = "catppuccin-mocha"  # TUI color theme (see Theme section)
notifications = false       # Desktop notifications for new mail (see Notifications section)

[email]
font_family = "Helvetica, Arial, sans-serif"  # HTML email font
font_size = "12pt"                             # HTML email font size
include_signature = true                       # Append signature to outgoing mail
send_hold_secs = 20                            # Undo-send window before hand-off (0 = send now)

[smtp]
host = "postout.lrz.de"
port = 587
username = "your-id"
default_from = "your.name@example.com"
# Password stored in OS keyring, not here

[imap]
host = "imap.example.com"   # Falls back to smtp.host if omitted
port = 993
username = ""               # Falls back to smtp.username if omitted
fetch_concurrency = 4       # Mailboxes fetched in parallel per sync (1-8)
# Password falls back to SMTP password if not set separately

[mailboxes.inbox]
server = "INBOX"            # IMAP folder name; its messages are stored
                            # under the key "inbox" in the account database

[mailboxes.archive]
server = "Archive"

[mailboxes.sent]
server = "Sent Items"

[[mailboxes.extra]]         # Additional mailboxes to sync
server = "Projects"`} />
      <p>
        The file has no signature keys: each signature is a Markdown file under
        <code>~/.config/mailypoppins/signatures/</code>. See <a href="#signatures-section">Signatures</a>.
      </p>
    </section>

    <!-- Section Breakdown -->
    <section>
      <h2>Section Breakdown</h2>

      <!-- theme -->
      <h3 id="theme-section">theme</h3>
      <p>
        Selects the TUI color theme by name (helix-style: themes are built in and
        picked in config, not defined in separate files). The key is top-level, so
        it must appear before the first <code>[section]</code> header. Available themes:
      </p>
      <ul>
        <li><code>catppuccin-mocha</code> — the default; used when the key is omitted (aliases: <code>catppuccin</code>, <code>default</code>)</li>
        <li><code>catppuccin-latte</code> — light variant (alias: <code>latte</code>)</li>
        <li><code>tokyo-night</code> (alias: <code>tokyonight</code>)</li>
        <li><code>terminal</code> — adapts to the terminal's own ANSI palette: the background shows through and colors follow your terminal theme (aliases: <code>transparent</code>, <code>ansi</code>)</li>
      </ul>
      <p>
        Names are case-insensitive. An unknown name does not fail: the TUI starts
        with the default theme and logs a warning to the activity log.
      </p>
      <ConfigExample code={`theme = "tokyo-night"`} />

      <!-- notifications -->
      <h3 id="notifications-section">notifications</h3>
      <p>
        Opt-in desktop notifications for new mail while the TUI is running
        (default: <code>false</code>). Like <code>theme</code>, the key is
        top-level, so it must appear before the first <code>[section]</code>
        header. When a background sync (including the automatic IDLE-triggered
        fetches) saves new emails to the inbox, one notification is shown per
        sync: <code>sender: subject</code> for a single new email, or
        <code>N new emails</code> when several arrive at once. Read-status
        updates, duplicate cleanup, and mailboxes other than the inbox never
        trigger a notification.
      </p>
      <p>
        Notifications use the platform's native tooling &mdash;
        <code>osascript</code> on macOS and <code>notify-send</code> on Linux
        &mdash; and degrade silently if the tool is unavailable.
      </p>
      <ConfigExample code={`notifications = true`} />

      <!-- [email] -->
      <h3 id="email-section">[email]</h3>
      <p>
        Controls the HTML rendering of sent emails. The font family and size are applied
        to the generated HTML when Markdown is converted for sending. <code>include_signature</code>
        determines whether the account signature is appended to a draft body on compose, reply, and forward.
        <code>send_hold_secs</code> is the undo-send window: in the TUI a send is held for that many
        seconds before it is handed to SMTP, and <code>u</code> cancels it in the meantime, leaving the
        draft untouched. It defaults to 20 seconds; set it to <code>0</code> to send immediately.
      </p>
      <ConfigExample code={`[email]
font_family = "Helvetica, Arial, sans-serif"
font_size = "12pt"
include_signature = true
send_hold_secs = 20`} />

      <!-- [smtp] -->
      <h3 id="smtp-section">[smtp]</h3>
      <p>
        SMTP settings for sending email. The password is stored in the OS keyring
        (macOS Keychain, Windows Credential Manager, or Linux Secret Service) under the
        key <code>smtp-password</code> (or <code>smtp-password-&lt;account&gt;</code> for named accounts).
        It is never written to the config file.
      </p>
      <ConfigExample code={`[smtp]
host = "postout.lrz.de"
port = 587
username = "your-id"
default_from = "your.name@example.com"`} />

      <!-- [imap] -->
      <h3 id="imap-section">[imap]</h3>
      <p>
        IMAP settings for fetching, syncing, and watching mailboxes. <code>host</code>,
        <code>port</code> and <code>username</code> fall back to their SMTP counterparts
        if omitted, so you only need to specify them when the IMAP server differs from SMTP.
      </p>
      <p>
        <code>fetch_concurrency</code> is how many mailboxes a single sync fetches in
        parallel, each on its own connection (default <code>4</code>, clamped to the
        range 1-8). A sync opens one connection per mailbox and overlaps their latency,
        so more mailboxes finish in about the time of one. Lower it toward <code>1</code>
        if a server throttles concurrent connections.
      </p>
      <ConfigExample code={`[imap]
host = "imap.example.com"   # Falls back to smtp.host
port = 993
username = ""               # Falls back to smtp.username
fetch_concurrency = 4       # Parallel mailbox fetches per sync (1-8)`} />

      <!-- Where data lives -->
      <h3 id="data-dir-section">Data directory (not configurable)</h3>
      <p>
        Mail, drafts, contacts cache, OAuth2 tokens, and logs all live under a single
        OS-conventional app data directory:
      </p>
      <ul>
        <li><strong>macOS:</strong> <code>~/Library/Application Support/mailypoppins/</code></li>
        <li><strong>Linux (incl. WSL):</strong> <code>$XDG_DATA_HOME/mailypoppins/</code> (default <code>~/.local/share/mailypoppins/</code>)</li>
      </ul>
      <p>Layout under the data directory:</p>
      <ConfigExample code={`mailypoppins/
  accounts/<name>/store.sqlite3          # synced mail: one row per message
  accounts/<name>/blobs/                 # message bodies and attachments, by hash
  accounts/<name>/drafts/                # your drafts, one .md file each
  accounts/<name>/attachments/<msg-id>/  # attachments a forward carries into a draft
  accounts/<name>/contacts-cache.json    # contacts index
  tokens/<name>.enc                      # OAuth2 tokens
  state.json                             # app state: each account's default signature
  logs/mailypoppins-YYYY-MM-DD.log`} />
      <p>
        <code>mp config show</code> prints the first three paths for every configured account.
        Synced mail is not a file tree: a message is a row in <code>store.sqlite3</code> plus
        content-addressed blobs, and what you read in the TUI is rendered from those. Drafts are
        the half that is still files, which is what you would symlink into an Obsidian vault:
        <code>accounts/&lt;name&gt;/drafts/</code>. The location is not user-configurable per
        account; override the entire data root with
        <code>MAILYPOPPINS_DATA_DIR=/some/path</code> for tests or portable installs.
      </p>

      <!-- [mailboxes] -->
      <h3 id="mailboxes-section">[mailboxes]</h3>
      <p>
        Three special-role mailboxes are required: <code>inbox</code>, <code>archive</code>,
        and <code>sent</code>. Each has a <code>server</code> field (the IMAP folder name).
        Synced mail lives in the account's store, keyed by the role name, which is also the
        mailbox segment of an <code>mp://</code> selector. The one directory left under the
        account's data directory is <code>drafts/</code>.
      </p>
      <p>
        Additional mailboxes can be added with the <code>[[mailboxes.extra]]</code> array
        syntax. They are included in sync alongside the three core mailboxes, and their key is
        the server name verbatim.
      </p>
      <ConfigExample code={`[mailboxes.inbox]
server = "INBOX"

[mailboxes.archive]
server = "Archive"

[mailboxes.sent]
server = "Sent Items"

[[mailboxes.extra]]
server = "Projects"

[[mailboxes.extra]]
server = "Newsletters"`} />

      <!-- signatures -->
      <h3 id="signatures-section">Signatures</h3>
      <p>
        A signature is one Markdown file in <code>~/.config/mailypoppins/signatures/</code>.
        The file name without its extension is the signature's name, so <code>work.md</code>
        is the signature <code>work</code> and creating a signature is creating a file. The
        account's default is recorded in the app's own state file
        (<code>&lt;data_dir&gt;/state.json</code>), so <code>config.toml</code> carries nothing
        about signatures at all.
      </p>
      <ConfigExample code={`~/.config/mailypoppins/signatures/
  work.md
  work-external.md
  personal.md`} />
      <p>
        Press <code>cs</code> in the TUI to manage them: the list shows every signature with
        the default starred, <code>Enter</code> sets or clears the default,
        <code>e</code> opens the selected file in <code>$EDITOR</code>, <code>n</code> creates
        one, <code>r</code> renames, and <code>d</code> deletes after a confirmation.
        <code>mp config show</code> prints the directory, the default, and a one-line preview
        of each file.
      </p>
      <p>
        HTML in a signature file (a rich signature exported from another mail client) is
        converted to Markdown when it is read, links kept as <code>[text](url)</code>, so the
        draft you edit and both parts of the sent mail are always Markdown, never raw HTML.
      </p>
      <p>
        The signature is appended to the draft body when you compose, reply, or forward,
        so it is visible and editable in the draft rather than added on send. An account
        with no default signature adds nothing. Use <code>-s &lt;name&gt;</code> to pick a
        signature or <code>--no-signature</code> to skip it for one draft.
      </p>
      <p>
        Signatures used to live in <code>config.toml</code> under
        <code>[accounts.signatures.*]</code>, inline as <code>text</code> or as a
        <code>path</code>. The first run after the upgrade copies those entries out to files
        and keeps the old default, then prints a notice. The tables are dead after that and
        can be deleted by hand; mailypoppins never rewrites <code>config.toml</code>.
      </p>
    </section>

    <!-- Multi-Account Setup -->
    <section>
      <h2>Multi-Account Setup</h2>
      <p>
        To manage multiple email accounts, use the <code>[[accounts]]</code> array syntax.
        Each account has its own <code>smtp</code>, <code>imap</code>, and <code>mailboxes</code>
        sections nested under it. The account <code>name</code> is used to identify it in
        commands, keyring keys, and the data-dir layout (<code>accounts/&lt;name&gt;/</code>).
      </p>
      <p>
        In the TUI, press <code>ga</code> to switch between accounts.
      </p>
      <ConfigExample title="Two-account example" code={`[email]
font_family = "Helvetica, Arial, sans-serif"
font_size = "12pt"
include_signature = true

# --- First account ---
[[accounts]]
name = "personal"
default_from = "me@example.com"
auth_method = "password"

[accounts.smtp]
host = "smtp.example.com"
port = 587
username = "me@example.com"

[accounts.imap]
host = "imap.example.com"
port = 993

[accounts.mailboxes.inbox]
server = "INBOX"

[accounts.mailboxes.archive]
server = "Archive"

[accounts.mailboxes.sent]
server = "Sent"

# --- Second account ---
[[accounts]]
name = "work"
default_from = "name@company.com"
auth_method = "oauth2"

[accounts.oauth2]
client_id = "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
tenant_id = "yyyyyyyy-yyyy-yyyy-yyyy-yyyyyyyyyyyy"

[accounts.smtp]
host = "smtp.office365.com"
port = 587
username = "name@company.com"

[accounts.imap]
host = "outlook.office365.com"
port = 993
username = "name@company.com"

[accounts.mailboxes.inbox]
server = "INBOX"

[accounts.mailboxes.archive]
server = "Archive"

[accounts.mailboxes.sent]
server = "Sent Items"`} />
    </section>

    <!-- Authentication Methods -->
    <section>
      <h2>Authentication Methods</h2>
      <p>
        Each account has an <code>auth_method</code> field that determines how
        credentials are handled. Three methods are supported.
      </p>

      <h3 id="auth-password">1. Password (default)</h3>
      <p>
        Passwords are stored in the OS keyring (macOS Keychain, Windows Credential Manager,
        Linux Secret Service). They are never written to the config file. Keyring keys
        follow the pattern <code>smtp-password-&lt;name&gt;</code> and
        <code>imap-password-&lt;name&gt;</code>. If the IMAP password is not set, it falls
        back to the SMTP password.
      </p>
      <p>Set or update passwords with:</p>
      <div class="code-inline">
        <Code code="mp config set-password" lang="bash" theme="catppuccin-mocha" />
      </div>
      <ConfigExample title="Password auth config" code={`[[accounts]]
name = "personal"
default_from = "me@example.com"
auth_method = "password"  # This is the default; can be omitted

[accounts.smtp]
host = "smtp.example.com"
port = 587
username = "me@example.com"`} />

      <h3 id="auth-oauth2">2. OAuth2 (IMAP/SMTP)</h3>
      <p>
        For Microsoft 365 / Exchange Online accounts that support IMAP and SMTP.
        Requires an Azure Entra ID app registration with <code>IMAP.AccessAsUser.All</code>
        and <code>SMTP.Send</code> delegated permissions. Tokens are cached at
        <code>~/.email-cli/tokens/&lt;account&gt;.json</code> and refreshed automatically.
        Access tokens last about 1 hour; refresh tokens expire after roughly 90 days of
        inactivity.
      </p>
      <p>Run the device code flow to authenticate:</p>
      <div class="code-inline">
        <Code code="mp config oauth2-login --account work" lang="bash" theme="catppuccin-mocha" />
      </div>
      <ConfigExample title="OAuth2 (IMAP/SMTP) config" code={`[[accounts]]
name = "work"
default_from = "name@company.com"
auth_method = "oauth2"

[accounts.oauth2]
client_id = "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
tenant_id = "yyyyyyyy-yyyy-yyyy-yyyy-yyyyyyyyyyyy"

[accounts.smtp]
host = "smtp.office365.com"
port = 587
username = "name@company.com"

[accounts.imap]
host = "outlook.office365.com"
port = 993
username = "name@company.com"`} />

      <h3 id="auth-graph">3. Graph API</h3>
      <p>
        For tenants that block IMAP and SMTP entirely. Uses the Microsoft Graph REST API
        for all mail operations. Requires an Azure app registration with
        <code>Mail.Read</code>, <code>Mail.ReadWrite</code>, and <code>Mail.Send</code>
        delegated permissions (plus <code>offline_access</code> for refresh tokens).
      </p>
      <p>
        Graph accounts use well-known folder names (<code>inbox</code>,
        <code>archive</code>, <code>sentitems</code>) instead of IMAP folder names.
        SMTP and IMAP sections are not needed.
      </p>
      <div class="code-inline">
        <Code code="mp config oauth2-login --account exchange" lang="bash" theme="catppuccin-mocha" />
      </div>
      <ConfigExample title="Graph API config" code={`[[accounts]]
name = "exchange"
default_from = "name@company.com"
auth_method = "graph"

[accounts.oauth2]
client_id = "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
tenant_id = "yyyyyyyy-yyyy-yyyy-yyyy-yyyyyyyyyyyy"

[accounts.mailboxes.inbox]
server = "inbox"              # Graph well-known folder name

[accounts.mailboxes.archive]
server = "archive"

[accounts.mailboxes.sent]
server = "sentitems"          # Not "Sent Items"`} />
    </section>

    <!-- Azure App Registration -->
    <section>
      <h2>Azure App Registration</h2>
      <p>
        Both OAuth2 and Graph authentication require registering an application in
        Azure Entra ID (formerly Azure Active Directory). Here is a condensed setup.
      </p>

      <ol class="setup-steps">
        <li>
          Go to <a href="https://portal.azure.com" target="_blank" rel="noopener noreferrer">portal.azure.com</a>
          and navigate to Microsoft Entra ID &gt; App registrations &gt; New registration.
        </li>
        <li>
          Name the app (e.g., "mailypoppins"), set supported account types to
          "Accounts in this organizational directory only", and leave the redirect URI blank.
        </li>
        <li>
          In the app's Authentication settings, scroll to Advanced settings and set
          "Allow public client flows" to Yes. This enables the device code flow.
        </li>
        <li>
          Under API permissions, add delegated permissions:
          <ul>
            <li>
              For OAuth2 (IMAP/SMTP): <code>IMAP.AccessAsUser.All</code> and
              <code>SMTP.Send</code> from Office 365 Exchange Online, plus
              <code>offline_access</code> from Microsoft Graph.
            </li>
            <li>
              For Graph API: <code>Mail.Read</code>, <code>Mail.ReadWrite</code>, and
              <code>Mail.Send</code> from Microsoft Graph, plus <code>offline_access</code>.
            </li>
          </ul>
        </li>
        <li>
          If permissions show "Needs admin consent", request your IT administrator to
          grant consent via the Azure portal.
        </li>
        <li>
          From the app's Overview page, copy the Application (client) ID and
          Directory (tenant) ID into your config's <code>[accounts.oauth2]</code> section.
        </li>
      </ol>
    </section>

    <!-- Keyring Management -->
    <section>
      <h2>Keyring Management</h2>
      <p>
        Passwords for the <code>password</code> auth method are stored in the OS-native
        keyring. The CLI uses the <code>keyring</code> Rust crate, which maps to:
      </p>
      <ul class="keyring-list">
        <li>macOS: Keychain</li>
        <li>Windows: Credential Manager</li>
        <li>Linux: Secret Service (e.g., GNOME Keyring, KWallet)</li>
      </ul>
      <p>Keyring keys follow this naming pattern:</p>
      <ul class="keyring-list">
        <li><code>smtp-password-&lt;account-name&gt;</code></li>
        <li><code>imap-password-&lt;account-name&gt;</code> (falls back to SMTP password if not set)</li>
      </ul>
      <p>To set or update a password:</p>
      <div class="code-inline">
        <Code code="mp config set-password" lang="bash" theme="catppuccin-mocha" />
      </div>
      <p>
        The command prompts you to select which password to update (SMTP or IMAP) and
        which account. The password is entered interactively and never echoed to the terminal.
      </p>
    </section>
  </main>
  <Footer />
</Layout>

<style>
  .page {
    padding-top: var(--space-16);
    padding-bottom: var(--space-16);
  }

  .page-header {
    margin-bottom: var(--space-12);
  }

  .subtitle {
    font-size: 1.125rem;
    color: var(--color-subtext0);
  }

  section {
    margin-bottom: var(--space-8);
  }

  .setup-steps {
    list-style: decimal;
    padding-left: var(--space-6);
    color: var(--color-subtext1);
    margin-bottom: var(--space-6);
  }

  .setup-steps li {
    margin-bottom: var(--space-3);
    line-height: 1.6;
  }

  .setup-steps ul {
    list-style: disc;
    padding-left: var(--space-6);
    margin-top: var(--space-2);
  }

  .setup-steps ul li {
    margin-bottom: var(--space-2);
  }

  .keyring-list {
    list-style: disc;
    padding-left: var(--space-6);
    color: var(--color-subtext1);
    margin-bottom: var(--space-4);
  }

  .keyring-list li {
    margin-bottom: var(--space-2);
  }

  .code-inline {
    background: var(--color-crust);
    border: 1px solid var(--color-surface0);
    border-radius: 8px;
    padding: var(--space-3) var(--space-4);
    overflow-x: auto;
    margin-bottom: var(--space-4);
  }

  .code-inline :global(pre) {
    margin: 0;
    padding: 0;
    border: none;
    background: transparent;
    font-family: var(--font-mono);
    font-size: 0.875rem;
    line-height: 1.6;
  }

  .code-inline :global(code) {
    background: none;
    padding: 0;
    border-radius: 0;
    font-size: inherit;
    color: inherit;
  }
</style>
