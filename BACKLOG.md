# Backlog

Index of open tickets. One file per item lives in [docs/tickets/](docs/tickets/); see [docs/tickets/README.md](docs/tickets/README.md) for the convention. Use the `ticket` fish function to add a new entry.

When a ticket is shipped: set `status: done` in the ticket file, add an entry to [CHANGELOG.md](CHANGELOG.md), and remove its line from this index.

## Now

> Architecture review 2026-08-06, follow-ups #0053 to #0064: [synthesis](.agents/handoff/2026-08-06_architecture-review-synthesis.md). Suggested order is #0053, #0054, #0055, #0056, then #0057 and #0058. #0053, #0054, #0055, #0056, #0057, #0058 and #0064 have shipped. Their post-ship reviews all passed and left deferred notes, which are filed as #0065 to #0071; #0065, #0066, #0067, #0068 and #0071 have shipped.

> Audit 2026-08-14, owner decisions across performance, UX/workflow and feature-survey: [synthesis](.agents/research/2026-08-14-audit-synthesis.md). Tickets #0087 to #0100 all shipped in 0.9.0 and after; #0101 is the only survivor, parked under Later.

> Preview latency on list navigation: [plan](docs/plans/preview-latency.md), four tickets in order, each measured before the next starts. All four have shipped: Ticket A ([#0108](docs/tickets/0108-coalesce-key-events.md), instrumentation and key-event coalescing), Ticket B ([#0109](docs/tickets/0109-retire-inline-image-rendering.md), retiring inline image rendering), Ticket C ([#0110](docs/tickets/0110-retire-auto-mark-read.md), retiring auto-mark-read) and Ticket D ([#0111](docs/tickets/0111-retire-rich-html-preview.md), retiring the rich HTML preview render). The measurement was waived and the cache-and-prefetch contingency closed as unnecessary.

> The fetch skip list that never converges: [plan](docs/plans/sync-skip-list.md), five tickets. [#0112](docs/tickets/0112-gate-message-id-rebind.md) is the one that stops the pain and has shipped, along with [#0117](docs/tickets/0117-rebind-gate-after-a-windowed-reset.md), the regression it left on the passes that finish a windowed UIDVALIDITY reset, and so has [#0116](docs/tickets/0116-one-outbox-drain-per-account.md), whose premise in the plan was wrong: the duplicate Sent copies were mp appending the same row from several concurrent drains, not Exchange filing its own copy, and so has [#0114](docs/tickets/0114-drain-mutation-queue-at-tail.md), which drains the mutation queue and the outbox at the tail of a tick as well as the head so a change made during a sync does not wait for the next one, and so has [#0113](docs/tickets/0113-bound-the-body-fetch.md), which chunks the body fetch and gives each mailbox a deadline so a slow one yields the tick instead of holding it, and so has [#0115](docs/tickets/0115-warn-on-a-non-converging-fetch.md), the detector that makes a fetch downloading the same messages pass after pass visible in the log, on the status line and in `mp sync`, written last so it was not tuned against the bug it would have caught. All five have shipped.

> Daemon-first architecture, on the way to a native GUI: [plan](.agents/workflow/native-gui-daemon/plan.md), eight tickets, one per phase, [#0118](docs/tickets/0118-pre-daemon-baselines-and-inventories.md) Phase 0 through #0125 Phase 6. The freeze is the `pre-daemon` tag (`f8af44b`) and the work lands on the `daemon` branch. #0118 has shipped: the committed baselines under [docs/baselines/pre-daemon/](docs/baselines/pre-daemon/), the 131-identifier [parity matrix](docs/parity-matrix.md), the engine-import boundary test, and the [gate evidence](docs/baselines/pre-daemon/gate.md). Two things carry out of it. Five measurement rows were `NOT TAKEN` on this host (preview p50/p95, cold first paint, mutation propagation, the TUI half of the 5000-row refetch, and the cold-cache CLI figure); P5-U11 closed cold first paint by defining the method and taking all three columns in one session ([phase5-gate-evidence.md](docs/baselines/phase5-gate-evidence.md)), and the other four are still owner action on a machine with a terminal and a real account. And the Phase 0 gate line asking every GUI-parity capability for a target interaction is answered only by the deferral list below, because the plan designs the GUI in Phase 9; that half of the line moves to the Phase 9 checklist. [#0119](docs/tickets/0119-daemon-risk-spikes.md) Phase 1a has shipped too: the four decision records under [docs/baselines/decisions/](docs/baselines/decisions/) (keep JSON-RPC, whole-list transfer with row deltas, a temp-file handle above 1 MiB, two read connections per account), the bootstrap ordering Phase 3a implements ([docs/plans/daemon-bootstrap.md](docs/plans/daemon-bootstrap.md)), and the dependency due diligence ([docs/plans/daemon-dependencies.md](docs/plans/daemon-dependencies.md)), which adopts nothing new. Two things carry out of it. `spikes/ipc-bench` is kept rather than deleted at the gate, because Phase 6 re-runs its workloads against the complete dispatcher and a rewritten benchmark is not the same benchmark; P6-U10 re-ran those workloads through the product binaries rather than through the spike, so the deletion is still open and is in the #0125 block below. And the two interactive numbers, the held-key preview A/B and cold first paint, are still owner action, unchanged from #0118.
> [#0120](docs/tickets/0120-daemon-transport-and-read-only-methods.md) Phase 2 has shipped, and it is the first one with product code in it: the workspace, `crates/mp-protocol` and `crates/mp-client`, the runtime files and the start lock, the `mp daemon` lifecycle commands, the `initialize` handshake, and the read-only `account.list` and `message.list` behind a hidden `--daemon`.
> All of it is behind the `daemon` cargo feature, so `cargo install --path .` ships an unchanged `mp` and `mp --help` is byte-identical to the pre-daemon baseline in both builds; the gate evidence is [docs/baselines/phase2-gate-evidence.md](docs/baselines/phase2-gate-evidence.md), the wire contract [docs/daemon-protocol.md](docs/daemon-protocol.md), and the operator's half [docs/daemon-operations.md](docs/daemon-operations.md).
> Two gate lines are not fully signed off here: the macOS half of "crash and stale-socket recovery pass on macOS and Linux" is escalated, since this is a Linux host, and the `config.*` half of the zero-config line moves to Phase 3b, which is where the plan puts that method family.
> Three follow-ups carry out of it, none of them blocking Phase 3a.
> The `account.list` and `message.list` fixtures are checked for canonical form, round-tripping and their documented field sets, but not against a live daemon's answer, where `initialize` is; the missing test is the read-only twin of `initialize_request_matches_the_pinned_fixture_shape`.
> The per-call timeout lives in `src/main.rs` around each `Connection::call`, so `mp-client` itself has none and a second consumer would have to reinvent it.
> And `mp-client` skips server-initiated notifications, because delivering `state.event` and `state.resync_required` needs an owned reader task rather than a borrowed one, which is Phase 3a's to build.
> [#0121](docs/tickets/0121-daemon-dispatcher-and-state-model.md) Phase 3a has shipped: the dispatcher every method now registers on, the canonical state with its revision counter and `state.bootstrap`, per-connection event delivery with coalescing and backpressure, the operation registry with `operation.status` and `operation.cancel`, and `mailbox.list` as the third read-only method.
> Still all behind the `daemon` feature, still byte-identical help in both builds; the gate evidence is [docs/baselines/phase3a-gate-evidence.md](docs/baselines/phase3a-gate-evidence.md), and the seven deviations are in the ticket rather than left to a diff.
> Six follow-ups carry out of it, none of them blocking Phase 3b; the double store open in `mailbox.list` and the clippy warnings in the T-unit test files were closed by the review-fix commit that followed the sweep.
> The bootstrap snapshot's `operations` projection is unit-tested against the registry but never taken over a socket while an operation is in flight, so the one shape a reconnecting GUI depends on is the one no end-to-end test covers.
> `Outbound::rebootstrap` clearing the lifecycle events an overflow keeps is closed by a rule rather than a queue change: re-query is the rule; holds from the snapshot, operations via `operation.status` (`docs/daemon-protocol.md`, the TUI's `requery_operations`).
> `daemon.status` reports no per-connection queue depth, which is the diagnostic an operator wants when a client falls behind and the reason bounded memory had to be pinned in process instead of over the wire.
> `EventQueue::try_recv`, `len`, `is_empty` and, since `drain_all` took over the connection loop, `drain` and `drain_lifecycle` are called only by the contract tests, and `Change::kind()` maps three kinds (`mailbox.counts_changed`, `draft.removed`, `outbox.counts_changed`) that no longer travel since `Event::from_change` supersedes them; both are for the first phase that touches those files, not a drive-by deletion.
> `mp-client`'s `StateTracker` treats any revision above `watermark + 1` as a gap, which coalescing makes wrong the moment a real method commits two mergeable changes: either the daemon carries the highest revision a merged entry superseded, or the client stops doing arithmetic and trusts `state.resync_required`.
> The per-call timeout and the fixtures-against-a-live-daemon gap carried from #0120 are both still open, and `mp-client` now does deliver notifications, which was the third of them.
> [#0122](docs/tickets/0122-daemon-ownership-moves.md) Phase 3b has shipped: the engine lock extended to the sync ingest, one account runtime per account holding that lock with its tick and its two-connection read pool, the typed `sync.completed` event with the CLI and TUI wordings derived from it in `mp-client`, daemon-owned configuration with an atomic reload and six `config.*` methods, the draft and signature watcher with `draft.approve`, and materialised handles that pin a blob against the retention sweep.
> It is the first phase with a change outside the `daemon` feature: `mp sync` and the TUI now refuse an ingest when another engine holds the account's lock, and say so. The gate evidence is [docs/baselines/phase3b-gate-evidence.md](docs/baselines/phase3b-gate-evidence.md), and the five deviations are in the ticket rather than left to a diff.
> Phase 3b narrows one #0121 follow-up without closing it: the `StateTracker` gap arithmetic is right for the lifecycle events this phase adds, because they are delivered densely, and still wrong for a coalesced Replace or Invalidate stream.
> The follow-ups it carries, none of them blocking Phase 4:
> `signature.changed` has no `signature.removed` twin, so a deleted signature file leaves a row every client keeps until it re-bootstraps.
> There is no `message.attachments` method, so a client that wants to materialise a part has to know the part index before it can ask for one.
> The watcher's roots are keyed by account directory and its inventory by draft id, so two drafts in one account carrying the same `id:` shadow each other with whichever settled last, silently.
> The `tests/engine_lock_ingest.rs` flake is closed by the review-fix commit that followed the sweep: the one test that forks an `mp` child now lives in `tests/engine_lock_ingest_cli.rs`, a target of its own that is still in the default suite, so no sibling thread holds an `flock`ed descriptor for that fork to duplicate.
> Three tick bodies gated on one `tokio::sync::Notify` is a shape the daemon tests keep rebuilding by hand and keep getting wrong; a small `Gate` helper with an explicit permit count belongs beside them.
> `RuntimeTable::blocked_reason` has no caller, `RuntimeTable::tick_account` none outside its own tests until the periodic scheduler arrives in Phase 5/6, and `store::sweep::sweep_pinned` none except `sweep` with an empty pin set. That last one sharpened in Phase 4 rather than closing: `diagnostic.store_gc` calls `sweep`, and P4-U15 routed the post-sync sweep through it, so the daemon is now the process that sweeps and it still passes no pin set. A blob backing a handle a client holds is therefore not protected from the daemon's own sweep, which is what `ANO-6` says it should be.
> The production `TickHooks` are the one part of the runtime no test drives: every contract test substitutes its own three callbacks, so the wiring from `production_hooks` to the real drains and the real sync is checked by inspection only.
> `ConfigReport` still lives in `session.rs` beside `ConfigState`, which now carries the same three cases; folding the first into the second would leave one spelling of "absent, loaded, invalid" instead of two that have to be kept in step.
> `config.get` renders the whole effective configuration, so a client interested in one account pays for every account and filters client-side; a per-account variant is the obvious second shape.
> `config.set_password` is tested against the encrypted-file backend only, because the keyring backend needs a session bus the test host does not have, and `config.reload` is not tested against a `config.toml` that vanished between the read and the swap.
> `src/store/sweep.rs` is not rustfmt-clean, with two `rustfmt --edition 2021 --check` hunks that predate #0122; the tree as a whole has never been clean, so this is the file to start with when that changes.
> Seven clippy warnings sit in two pinned test files, `tests/daemon_draft_watch.rs` 5 and `tests/daemon_config.rs` 2, all mechanical (`doc_lazy_continuation`, `cmp_owned`). They were left rather than fixed because the sweep's approval named `tests/daemon_account_runtime.rs` only.
> `docs/architecture.md` describes the `daemon` feature and the two client crates but has no table row for the files under `src/daemon/`, which is now twenty-four files across four directories and the largest undescribed part of the tree.
> `mp sync` and the TUI still build their own sync wordings in `src/main.rs` and `crates/mp-tui/src/helpers.rs` while `crates/mp-client/src/format.rs` builds the same ones from the event payload, so the two can drift until Phase 4 routes the CLI through the client.
> The `↻ mutations: N completed, M failed` line `mp sync` prints has no equivalent in the `sync.completed` payload: the payload carries `failed_mutations` and nothing about the mutations that succeeded, so a client cannot render that line at all, and the outbox line beside it is missing for the same reason. P4-U10 answered this one: the drain counts travel on the pass's `operation.progress` reports, one per phase, and `mp_client::format` renders both lines from them.
> `mp watch --mailbox` is narrowed to INBOX (P4-U10, the route [the plan](.agents/workflow/native-gui-daemon/plan.md) recommends for P4-U9). The daemon's watcher is INBOX-only, as `imap_watch` has always been, and serving an arbitrary mailbox needs an on-demand client-scoped IDLE connection: a second connection class with its own teardown semantics, for a flag with no known user. Until it is built, `mp watch --mailbox Archive` watches INBOX, says so in one line on stderr before it calls, and the daemon refuses anything but INBOX with `-32602`, so the narrowing is visible from both sides. This entry is the one the warning text names.
> A **fake IMAP backend fixture** is the single thing standing between two slices and their success paths. `tests/daemon_mutation_slice.rs` cannot reach the local commit, the synchronous drain or the rollback a refusing server causes, and `tests/daemon_sync_slice.rs` cannot reach a sync that ingests, a server mailbox list, a server message list, an IDLE that fires or `mp watch --timeout N` reaching exit 2; both pin the refusals instead and say so. The repository has never had one (`rg -l 'fake_imap|FakeImap|MockImap|imap_server' tests src crates` finds nothing) and the nearest thing is `tests/engine_lock_ingest_cli.rs`'s sentinel `TcpListener`, which exists to prove that *no* connection was made.

> [#0123](docs/tickets/0123-cli-cutover.md) Phase 4 has shipped: the CLI is off the direct path. The `daemon` cargo feature is gone, a normal client starts a daemon on demand, and every command a slice contracted answers from it byte-identically to the `pre-daemon` binary, refusals included; six slice suites (read, draft, mutation, sync/watch, send, admin) diff the two binaries over seeded roots. P4-U15 then deleted the direct engine path each of those commands used, 1 029 lines across eight files, and moved the post-sync retention sweep onto `diagnostic.store_gc`. The gate evidence is [docs/baselines/phase4-gate-evidence.md](docs/baselines/phase4-gate-evidence.md), and the deviations are in the ticket rather than left to a diff.
> The gate line "no command opens a store, a secret backend, a network backend or an engine lock" is at seventeen rows rather than zero, recorded in `CLI_ENGINE_RESIDUE` (`tests/architecture_boundaries.rs`) with a reason each, in four groups, and each group is a follow-up below.
> The follow-ups Phase 4 carries, none of them blocking Phase 5:
> The **server leg of `mp search`** (`docs/parity-matrix.md` LST-06) is unmigrated: the read slice contracted `mp search --local` and nothing else, so a server search still opens its own IMAP session or Graph client in `src/main.rs`, and the plain-IMAP `has:attachment` post-filter still reads the local index there. The method it wants is `message.search_server`, the twin of `message.list_server`. It is the one whole command surface Phase 4 leaves on the direct path.
> IMAP SEARCH sends non-ASCII values as raw 8-bit inside quoted strings without `CHARSET UTF-8` (RFC 3501 disallows); strict servers answer BAD; needs CHARSET + literal syntax (`crates/mp-core/src/search.rs` `to_imap`).
> The **two `config.toml` wizards** still prompt, dial a server, store the password they prompted for (`secrets::set_secret`, `src/config_cmd/init.rs:227,286,633,673`) and write the file in the client before calling `config.reload`. What they ask the daemon for is `config.get`, through `routed_config_state`; what has no wire shape is the prompting loop whose intermediate results steer the next prompt, and building one is a wizard protocol rather than a method.
> `config.init` and `config.add_account` are served and are what that protocol would build on, but **no wizard calls either**: their only exercise is `tests/daemon_config.rs`. A method with no product caller is a method that can drift from the wizard it exists for, so the wizard protocol above is what retires the gap.
> A draft's relative `attachments:` entries are anchored to **the draft file's own directory** rather than to the sender's cwd, since no attachment path crosses the wire and the daemon's cwd is nobody's choice. It is an accepted divergence from the pre-daemon binary (`docs/parity-matrix.md` ATT-03, pinned by `tests/daemon_send_attachments.rs`). What is still cwd-anchored is the *warning*: `draft::validate_draft` checks an entry's existence with no anchor at all, so a valid draft whose attachment sits beside it is reported `Attachment not found: report.pdf` by `mp validate` and in the send preview, from inside the daemon. Same anchor question, different function, and it moves bytes on `mp validate`, so it wants a row of its own.
> **`mp config show`'s secret column, token line and signature listing** are still local reads. `tests/daemon_config.rs` contracts `config.get` *not* to probe a secret, and `tests/daemon_admin_slice.rs` pins the config family at nine methods, so closing this needs a decision about whether a `config.*` method may probe a secret at all, not an implementation.
> The **startup preamble** (`init_secrets_backend` plus the `SmtpConfig::load` under it) prints the `⚠ Could not load SMTP config` pair before any socket and on the no-daemon list too, so it cannot route without moving bytes on `mp config path`. `mp send-approved` prints the same line per account, one round trip too early to come off `send.approved`, and `config.get`'s key set is pinned at four so it cannot carry an `smtp_warnings` field either.
> **`mp dump-mailbox --json` is slower routed than in process**, because a multi-MiB answer costs more over the socket than the 33 ms the daemon saves per call. It is the one command that pays more than it saves and the first candidate if a streamed or handle-backed NDJSON answer is ever built; every small query is 3-4x faster. Phase 4 measured 121 ms against 86 for 5 351 records on tmpfs, Phase 6 measured 116 against 91 on ext4, so it is 1.27x against W3's 1.2x rule and narrowing rather than closed.
> **No test drives two `mp` processes concurrently against one daemon**, which is one of Phase 4's six gate lines. What exists is sequential reuse (`a_second_client_reuses_the_daemon_the_first_one_started`) and two clients racing to *start* one (`two_concurrent_starts_yield_one_pid_file_and_one_instance_id`).
> The plan's **runtime assertion that the client process holds no `store.lock`** has no row of its own; `tests/engine_lock_ingest_cli.rs` proves the property from the other side, by holding the lock in the test process and asserting the forked `mp sync` skips.
> `collisions` is reported only on `draft.list`; the other `draft.*` methods scan the same directory and could see the same colliding ids, and say nothing.
> `draft.list` and `draft.validate` each parse the draft file twice, once for the projection and once for the diagnostics.
> `resolve_body_signature` is still a delegate rather than a method of its own, so the signature resolution a send needs is reached through the draft path.
> `draft.create` takes the from-address from `account.default_from` directly rather than through the configuration accessor the rest of the family uses.
> The per-call daemon timeout (`DAEMON_TIMEOUT`, derived from `client::CONNECT_TIMEOUT`) still lives in `src/main.rs` rather than in `mp-client` (carried from #0120).
> The connect-bootstrap-call-settle sequence every routed command opens with belongs in a `crates/mp-client` session helper; `src/main.rs` has `daemon_connection`, `operation_session`, `run_admin_operation` and `settle`, and a second consumer would have to reinvent all four.
> A sync pass with a `--mailbox` subset or `--dry-run` takes the guarded path even when this daemon holds the account's engine lock, because the runtime's tick carries neither. Since P5-U8 that path is not a refusal any more: the guarded pass takes a turn at the process's own engine gate (`EngineLock::take_turn_at`) and runs. What is still owed is a tick that can carry a mailbox subset and a dry run, so a narrowed pass goes through the runtime like every other.
> **An account with no local store gets no runtime and comes up `blocked`** (P5-U8), because opening a store creates one and an empty database would turn every read method's `-32006` into an empty answer for an account the user has not synced. The first `mp sync` creates the store and the runtime appears at the next daemon start; what is missing is the daemon noticing, which is a re-check after a pass that created a store.
> **The daemon's watcher is a watch and not a scheduler** (`src/daemon/runtime/watcher.rs`): it reacts to an IDLE round or a Graph poll seeing the mailbox move, exactly as the TUI's two threads did. A periodic tick that keeps a store fresh with no client anywhere is **not built**: the plan put it in Phase 5/6 and neither phase built one, so it is an item here rather than a phase's, and so is #0042's Graph delta query, which is what would stop the poller enumerating the whole inbox every minute.
> **`mp_client::StateTracker` is stricter than the protocol**: any jump above `watermark + 1` is a gap, which is exact only on a stream that coalesced nothing. The TUI drains a coalescing queue since P5-U8 and is the first client that could be bitten; it applies the watermark itself rather than through the tracker, so nothing is wrong today and the tracker is what wants the fix.
> `Outbound::rebootstrap` drops lifecycle events (`config.changed`, `daemon.shutting_down`) whose revision is at or below the snapshot; keeping them needs a protocol decision (clients apply lifecycle events at or below the watermark without moving it) plus `mp-client` `StateTracker` and `mp-tui` `events.rs` changes.
> `send.approved` and `send.outbox_retry` settle with the whole outcome and render it at the end, where the pre-daemon binary printed a line per message as it went; nothing pins the interleaving, but per-progress rendering is what a GUI will want and the progress events are already there.
> `contact.search`'s cache-guard refusal (a rebuild that would shrink or empty the index) is only logged on the daemon side, so a client sees a successful search over the kept cache and no sign that a rebuild was refused.
> `mp contacts search` and `mp contacts stats` against a storeless account now refuse with `-32006` where they printed an empty index. No parity row covers the case; the alternative was a method answering about an account `account.list` calls not ready.
> `mp config oauth2-login`'s post-login IMAP, SMTP and Graph connection tests were dropped (P4-U14): they need the access token, which the daemon holds and the settled result deliberately does not carry. They want a home in the account slice.
> `RuntimeTable::remove` hands back the `Arc` it removed, so a caller holding one keeps the runtime alive past the removal; nothing does today, and the removal's contract should say which it is.
> `tests/daemon_draft_slice.rs`'s header and one assertion message say the family serves "nine" methods, which stopped being true when P4-U7 added `draft.discard`; the assertion compares against `DRAFT_METHODS` and is right, only the wording is stale.
> `docs/architecture.md`'s test count drifts every phase (1 817 before this one, 2 071 after); it is a number in prose with nothing checking it, unlike the five floors `tests/test_selection_guard.rs` defends.

> [#0124](docs/tickets/0124-tui-cutover.md) Phase 5 has shipped: the TUI is off the direct path. The six `open_store` call sites became typed queries, every `Action` became a daemon method or a documented client-only one, the two watcher threads became the daemon's account runtime watcher, and the parity gate is green, five oracles and all. The gate evidence is [docs/baselines/phase5-gate-evidence.md](docs/baselines/phase5-gate-evidence.md), the manual key checklist [docs/baselines/phase5-manual-keys.md](docs/baselines/phase5-manual-keys.md), and the deviations are in the ticket rather than left to a diff.
> **P5-U10, the crate boundary for the TUI, did not land with Phase 5 and took eleven units rather than one** (it has since landed; see #0126 under Phase 6). It landed its first third (the three invitation reads through `calendar.events`, `message.ics` and `message.invite`) and found that `crates/mp-tui` needs about 15 000 lines moved across sixteen shared modules first, six of which need a genuine split, none of it new production code and all of it a tree that does not compile between the first `git mv` and the last. The sequencing is **P5-U10a** (a shared crate holding the engine-free closure: `app_state`, `calendar`, `config`, `notify`, `oauth2`, `parse`, `secrets`, `signatures`, `sync_health`, `timing`, `types`, plus splitting `selector`, `search` and `invite`), **P5-U10b** (`RD-06`, `RD-07`, `LST-08`, `LST-09` and the wire-row types, splitting `reconcile`, `contacts` and `draft` on the way past) and **P5-U10c** (the move itself and the test modules that link the engine), each leaving `cargo test --workspace` green. They run **after Phase 6**, because P6-U1/U2 remove one of the eleven allow-list rows on their own and nothing in Phase 6 depends on the crate boundary.
> The **engine-import allow-list was at 11 rows** (`tests/fixtures/tui-engine-imports.txt`) where the plan asks for zero, in four groups with the surface each one waited on, and `TUI_ACTION_ENGINE_RESIDUE` at 8 function-level rows beside it. Both are empty now; `docs/architecture.md`'s TUI-layering section describes what they became.
> **`LST-08`, the TUI's server search leg, was unmigrated**, the twin of Phase 4's `LST-06`. P5-U10c-I1 served it as `message.search_server`, a durable operation that streams a hit per event; `LST-06` itself is still on the direct path, below.
> **The daemon called into the TUI**: `calendar.events` was served by the TUI's own agenda loader. P5-U10c-I2 resolved it by lifting the loader into `src/agenda.rs` with its 25 tests, and P5-U10e did the same for `mailbox.list`'s Drafts count.
> **`App::load_draft_body` opened a store with `Store::open`**, so `TUI_APP_STORE_RESIDUE`'s scan for `open_store(` never saw it. `draft.path` plus a client-side parse is what it became in P5-U10b, and the reader itself is the oracle in `src/tui_tests/oracle.rs`.
> **The uid index is a workaround for a missing field.** The query layer keeps a process-wide `(account, mailbox) -> (uid -> id)` table because a held list is keyed by `messages.id` and the daemon removes a row by `(mailbox, uid)`; a `uid` on `EmailEntry` is the shape that would delete it, and it is 31 struct literals across seven files, two of them the frozen golden-frame fixtures.
> **`src/main.rs` has a second wire-row decoder**, `row_from_wire`, beside the query layer's `entry_from_row`; they die into one when the row types are protocol types.
> **A materialised attachment or browser rendition now lives ten minutes**, the handle lifetime, where the store-backed helper wrote into a per-row directory that lived until the temp tree was swept. Neither is released, for the reason `ATT-01` already gave for `mp open`: the viewer that was just launched is holding the file.
> **`DFT-11`'s recipient rewrite is still client-side**: `ce` resolves the file through `draft.path` and then writes the frontmatter itself, because `draft.set_recipients` is not built. It costs a second drafts-index refresh the daemon could have done in one.
> **`message.get` computes the attachment list and the selector for every preview**, because its result is a whole `ShownMessage`. It is inside the W1 budget and it is one query more than the pre-daemon read did.
> **An empty stored body reads as `Some("")` through the store and `None` through `message.get`**, which `shown_message` filters, so no frame moves; a shape that cared would need `message.get` to stop filtering.
> **The TUI announces itself as `ClientKind::Cli`**, because it reaches the daemon through `client_session`, the door every routed command uses. `ClientKind::Tui` exists and only the undo-send-hold suite and the in-process fixtures send it, so the daemon cannot tell a TUI from a one-shot command, which is what P6-U3's "when the last client exits mid-hold" has to be stated over.
> **No automated test drives a TUI and a CLI against one daemon at the same instant.** The concurrent case was taken by hand during the manual key walk and is recorded in the gate evidence; the automated half is the same gap Phase 4 recorded for two concurrent CLI commands.
> **`b` on a search hit whose message carries no HTML puts nothing on the overlay's status line.** The refusal (`message.materialise_html`, `-32602`) reaches the log and the same key outside the overlay says so on screen, which is what `a_message_without_html_says_so_instead_of_opening_an_empty_page` pins.
> **Three of the twenty `KeyAction::Manual` keys are `NOT TAKEN (owner-only)`** and want a host with a display, a browser and a real account: `y` (the clipboard, which `arboard` cannot reach headless), `f` (a server-only search hit, which an offline fixture cannot produce) and `b` (a message with an HTML part). The fake IMAP/SMTP pair under Later is what would close `f` without a real server.

> [#0125](docs/tickets/0125-daemon-hardening.md) Phase 6 has shipped, and it is the phase that makes the daemon something you can leave running: the undo-send hold is the daemon's scheduler rather than a timer in whichever client was open, `mp daemon stop` cancels the holds, announces itself, settles what is running inside a grace and names what it could not, `mp daemon install-service` writes a systemd user unit or a launchd agent, `mp daemon health | logs | support-bundle` answer for the daemon itself, and seven soak rows hold it under sustained load. The gate evidence is [docs/baselines/phase6-gate-evidence.md](docs/baselines/phase6-gate-evidence.md), the soak run [docs/baselines/phase6-soak.md](docs/baselines/phase6-soak.md), and the deviations are in the ticket rather than left to a diff.
> **P5-U10 has landed whole**, in eleven units tracked in [#0126](docs/tickets/0126-tui-crate-move.md): `crates/mp-tui` is the TUI, it depends on `mp-core`, `mp-client` and `mp-protocol` and names no `mailypoppins`, and both engine allow-lists are empty. What follows is what each unit found, kept because the next boundary someone draws will meet the same shapes.
> **P5-U10a and P5-U10b built the shared crate.** `crates/mp-core` holds the engine-free closure the TUI reaches: eleven modules whole (`app_state`, `calendar`, `config`, `notify`, `oauth2`, `parse`, `secrets`, `signatures`, `sync_health`, `timing`, `types`) and the engine-free half of `selector`, `search`, `invite`, `reconcile`, `contacts` and `draft`, plus `addresses` and `imap_query`, all re-exported from the root crate under their old paths so no call site outside the moved files changed.
> **P5-U10c-I1 built the four surfaces P5-U10b did not.** `message.materialise_markdown`, the `selector` on a listing row, and `message.search_server` / `message.fetch` as two durable operations.
> **P5-U10c-I2 took the contacts refresh, the agenda and the invite blob**, and measured what the `git mv` really costs. `App::refresh_contacts` is `Action::RefreshContacts` and `contact.rebuild`; the agenda loader is `src/agenda.rs`, in the crate that owns the store it reads, answering `mp_protocol::calendar::AgendaEvent` to `calendar.events` and to the TUI alike; the `OpenEventSource` arm reads its `invite.ics` through `message.ics`. `tests/fixtures/tui-engine-imports.txt` is six rows and `TUI_ACTION_ENGINE_RESIDUE` is **empty**.
> **The import allow-list was not what measured the move**, which is why `tests/fixtures/tui-engine-paths.txt` exists (P5-U10d-T): the import scan reads `use` statements, and the calls that block `crates/mp-tui` are mostly fully-qualified paths it never sees, fifteen of them over six groups where the import list read six.
> **P5-U10d-I closed four of those six groups.** `message.thread` serves the conversation overlay, which was the last read in the TUI's app module no method answered; `draft.create_from_message` serves the reply and the forward of a server-search hit that resolved to no local row, which is the one draft `draft.reply` and `draft.forward` cannot build because every form of their `source` is an address into the store; the one-second drafts poll and its three `store::drafts::refresh_account` calls are gone against the daemon's watcher and `draft.list`'s fresh directory scan; and the outbox badge was a deletion, because `AccountState::outbox` was written on every account open and every sync completion and read nowhere since the status bar took the badge with it. Both allow-lists are at five rows.
> **`w` on a server-only search hit forwards no attachments**, which is a behaviour difference against the pre-daemon build rather than a decision. Before P5-U10c-I1 the TUI ran the IMAP search itself and `hit.fetched.attachments` held the real parts, so a forward built from a hit carried them; since it, the client holds an envelope and two body renditions and no parts at all, `commands::hit_entry` sets `attachments: Vec::new()`, and `draft.create_from_message` (`DFT-09`) preserves what the client does rather than what it did. Closing it means materialising the parts, which is `message.fetch` plus `draft.forward` and a round trip the quote deliberately avoids; the alternative is saying so on the overlay's status line.
> **P5-U10e closed the last two groups by moving them rather than serving them.** The sessionless oracles are `src/tui_tests/oracle.rs`, in the crate that owns the store, with the 165 tests that read one: the two equality suites, the store-backed half of the list and counts, the three store-backed action modules, the command layer, the events, the undo-send hold, the invitation card and the twenty-three daemon-backed golden frames. The `App` kept no store-backed branch at all, which is P5-U8's "no direct fallback" made structural, and `src/daemon/methods/mailbox.rs` stopped counting the Drafts mailbox through `crate::tui::app::draft_count`: `indexed_drafts` and `draft_count` are `src/draft.rs`'s, beside the four other draft operations that need a store.
> **P5-U10f is the move, and it decided three things.** The connect helper is injected: `Session::connect` takes a `Connector` of two function pointers and `daemon::client::tui_connector` builds it, so there is one connect routine in the tree and the exit-4 diagnostic stays in the process that owns the terminal. `DraftFromSource` moved to `mp_core::draft`, beside the `SourceMessage` whose kind it names. And `ENGINE_MODULES` dropped `secrets` and `oauth2`, five units after they moved to `mp-core`: a `crate::` scan cannot see an `mp_core::` path, so the gate for those two is a text scan of the TUI crate for their new spellings.
> **`mp-tui` carries a `test-support` feature, `mp-core`'s seam a second time.** The sessionless `App` constructor, the two preview primers, the preview-span counter and the golden-frame fixtures were `#[cfg(test)]` and `src/tui_tests/` needs all four; resolver v2 keeps a dev-dependency's features out of `cargo build`, and `cargo build -v` against `cargo test -v` is the proof.
> **The daemon still reaches into `mp-tui` for two pure helpers.** `src/daemon/methods/message.rs` and `src/daemon/state/snapshot.rs` call `build_mailboxes` and `resolve_date`, which compile because the root crate depends on `mp-tui`; the arrow points the wrong way for a server calling into a client, and the shape is P5-U10c-I2's agenda inversion in miniature. Both are pure functions over config and strings, so the fix is a lift into `mp-core` rather than a method.
> **`mp search`'s server leg is still on the direct path** (`LST-06`, five rows of `CLI_ENGINE_RESIDUE`), and `message.search_server` now exists for it. Three user-visible behaviours stand in the way, each named in the residue table's reason and in the matrix row: the per-mailbox failure line printed to stderr as the search goes, `--mailbox` naming a server mailbox the account does not configure, and the plain-IMAP `has:attachment` warning.
> **The live systemd check is owner action and NOT TAKEN.** Every `systemctl` in Phase 6's evidence was a dry run or a stub `systemctl` recording its argv, so the unit is written and byte-identical to the committed fixture and the command sequence is pinned, but nothing proves systemd accepts the unit, that `enable --now` starts a daemon, or that the daemon comes back at the next login. On this host, with the dry run unset: `mp daemon install-service`, `systemctl --user status mailypoppins.service`, log out and back in, `mp daemon status` reporting a running daemon, then `mp daemon uninstall-service`. No test takes it because a real `enable --now` starts a daemon against the developer's own data directory.
> **The live launchd check is owner action and NOT TAKEN.** `launchctl` and `plutil` are both absent from this host, so the plist is pinned against a committed fixture and proves its bytes and nothing else. Install the service on the Mac, log out and back in, `mp daemon status`, then uninstall. Two questions ride on it: whether `bootstrap` refuses a label it has already loaded (a re-install would then want a `bootout` first, which no row pins), and what `current_exe()` resolves to under a Homebrew `mp`, which decides whether the agent survives a `brew upgrade` (`docs/release-process.md`).
> The follow-ups Phase 6 carries, none of them blocking the crate move:
> **`Snapshot::holds` and `Snapshot::operations` are `Vec<Value>`** while the event, `send.hold_status` and `operation.status` are all typed. P6-U4 kept them untyped deliberately, because a client reads a hold off the event rather than off the snapshot; the moment the GUI reads one *there*, the shape is pinned from the right end and the two fields should follow.
> **`AccountView.health` is dead**: it is only ever `SyncHealth::default()`, so every `state.bootstrap` reports an `unknown` sync health for every account, whatever the last pass did. P6-U8 found it and did not reduce `sync.completed` into it, because `tests/daemon_sync_outcome.rs` pins that a completed sync moves no snapshot; the daemon keeps a `last_sync` map beside the canonical state instead, and the snapshot field is either filled from that or removed.
> **`DAEMON_ENV_HOOKS` clears twelve of the fifteen hooks** (`tests/support/parity.rs`). The two service hooks and `MAILYPOPPINS_DAEMON_FAKE_TRANSPORT` are outside the list because the suites that use them set them on every child; each omission was decided on its own and the list should say so once, or carry all fifteen.
> **The activity overlay has no level filter.** Its `/filter` matches the level's `Debug` spelling as a substring of the same query that matches the message (`crates/mp-tui/src/ui/activity.rs`), so `warn` selects the warnings *and* every line with the word in it, and "errors only" cannot be asked for at all. The diagnostics that land there since P6-U8 are what makes it worth having.
> **No protocol fixture pins a `state.bootstrap` whose `snapshot.diagnostics` is non-empty.** `crates/mp-protocol/fixtures/state.bootstrap.response.json` carries an empty array, which was the only possible value before P6-U8 filled it; a second fixture with one failing check is what would pin the shape a client renders.
> **Every `mp daemon` subcommand inherits the global `-s/--signature`, `--no-signature` and `-A/--account` flags**, because they are `global = true` on `Cli`. `mp daemon status -A alpha` is accepted and means nothing, which is a help screen that lies about what a lifecycle command takes.
> **`spikes/ipc-bench` is still in the tree.** It was kept past the Phase 1a gate so Phase 6 could re-run its workloads; P6-U10 re-ran the Phase 0 workloads through the product binaries instead, which is what "against the complete dispatcher" means now that there is one, so the spike bought nothing and its deletion is an unasked-for `git rm` waiting for a decision.

Settled deferrals for the daemon migration, recorded here because the Phase 0 gate points at this list:

- The first GUI release is dark-only. The light theme is deferred, which is the one `deferred` status in the parity matrix (`OBS-07`); `theme` stays a top-level `config.toml` key read once at startup until then.
- `mp fetch` (`LST-11`) keeps its command surface through the migration. Whether to deprecate it in favour of `sync` plus `search` is deferred to its own ticket rather than decided inside the cutover (`ANO-3`).
- `schemars` is not adopted now. Phase 2 pins the protocol with checked-in JSON fixtures, and the JSON Schema question reopens at Phase 7, the first consumer.
- `notify` is not adopted. The daemon's draft, signature and config watcher uses the 1-second fingerprint poll the TUI already runs, moved into the daemon, and a filesystem-notification crate is revisited only if that poll proves inadequate under test.
- `uuid` stays transitive. Ids are minted from `rand` plus a hex format, so no RFC-4122 shape and no new direct dependency.
- `SyncResult.bodies_truncated` stays a count, and so does the `bodies_truncated` field of the daemon's `sync.completed` payload, which P3b-U6 shipped carrying the data that exists. Widening either to the list of deadline-stopped mailbox names is a behaviour change to the engine and to the wording contracts of #0113 and #0115, it moves a wire field from `u64` to an array and therefore needs a protocol-changelog entry, and it gets its own ticket rather than riding along with the daemon's sync event.
- The capabilities #0109, #0110 and #0111 retired (inline images, auto-mark-read, the rich HTML preview) are not restored by the GUI. Their identifiers stay reserved in the matrix so a later document cannot rebind them.

## Next

> Data-access-layer redesign (DECIDED 2026-07-14, decisions settled 2026-07-31): server-as-truth SQLite mirror + content-addressed blob store; drafts local-only, received read-only. Greenfield rebuild on a branch, no dual-write, safety net is `mp-legacy` + the `pre-dal-nuke` tag. Plan: [docs/plans/data-access-layer.md](docs/plans/data-access-layer.md). Stage 0 (#0049, the pre-nuke oracle capture and the `pre-dal-nuke` freeze) is done. Order below is the build order; the stop-gate sits after the #0038 + #0050 + #0052 triple, because the product is only half usable between them. #0038, #0050 and #0052 have all shipped, so the stop-gate is reached and the stages below it are the work after the pause.

## Later

> TUI multi-view roadmap: [docs/plans/tui-restructure-views.md](docs/plans/tui-restructure-views.md). All three views have shipped: foundation (#0032), view switcher + Contacts (#0033), local calendar (#0034).

- [#0081 QRESYNC, UIDPLUS, and advancing the modseq on a capped pass](docs/tickets/0081-qresync-uidplus.md) -- perf _(the split-out half of #0041, which shipped the session pool and the CONDSTORE delta)_
- `message.list` with `limit: null` (the TUI's LoadMailbox) builds a 17-key `serde_json::Value` per row and re-parses `date_display` in `resolve_date`; at 50k rows that is ~150-250 ms on a Tokio worker and ~15-20 MB of JSON. Serialise a borrowing struct, format `date_sort` instead of re-parsing, page from the TUI, and run the read in `spawn_blocking` (`src/daemon/methods/message.rs` `list_rows`/`to_json`). -- perf
- [#0085 On-open re-fetch of an evicted body](docs/tickets/0085-on-open-body-refetch.md) -- feature _(the missing half of #0060, whose eviction sweep shipped; required before lowering a cap below the working set)_
- [#0101 Conversation-view collapse and inline navigation on top of the thread view](docs/tickets/0101-conversation-view-collapse-inline-nav.md) -- feature _(cross-ref #0008)_
- [#0084 iMIP send-side updates and cancellations](docs/tickets/0084-imip-send-cancel-and-update.md) -- feature _(the split-out send half of #0031, whose receive half shipped)_
- A real fake SMTP/IMAP pair for the test suite: a TLS listener plus a certificate generator, so a send can be driven end to end over the wire the product actually speaks -- chore _(three slices want it and none of them can have it: `tests/outbox_integration.rs` fakes the Sent mailbox behind the `SentMailbox` trait and never opens a socket, `tests/imip_integration.rs` parses and ingests but never sends, and `tests/daemon_send_slice.rs` (P4-U12) had to arm `MAILYPOPPINS_DAEMON_FAKE_TRANSPORT` in the daemon because `send::build_smtp_transport` is TLS-only on both branches. A plaintext `TcpListener` cannot serve it and a TLS one needs a certificate generator this tree does not depend on, so every routed success path is pinned against an in-process fake rather than against a server, and the SMTP conversation, its error strings and its timeouts stay unpinned.)_

### Distribution / cross-platform (adoption track)

> Windows is targeted via WSL only. Native Windows (msvc, Credential Manager, Scoop, winget, EV signing) is out of scope.

- [#0012 Apple Developer ID signing for macOS releases](docs/tickets/0012-apple-developer-id-signing.md) -- chore
- [#0014 Linux packaging (.deb, .rpm, AUR, musl)](docs/tickets/0014-linux-packaging.md) -- chore
- [#0015 Cross-platform smoke tests](docs/tickets/0015-cross-platform-smoke-tests.md) -- chore

## Parked (Graph)

> Graph backend parked (decision 2026-08-06): nothing depends on it today, and the priority is features and stability on the IMAP/SMTP path.
> It wakes deliberately, not opportunistically: the first live target is the EVOQS Exchange account, the items below are picked up together, and the pre-live-contact checklist (end of [0065-followup-report](.agents/workflow/0065-followup-report.md), also the Verification section of [#0065](docs/tickets/0065-graph-prune-batch-hardening.md)) is run before that first contact.

- [#0035 Graph API admin approval + Azure app verification](docs/tickets/0035-graph-admin-approval.md) -- chore _(blocked; written against the TUM tenant, re-scope for EVOQS on wake)_
- [#0036 Graph sync backend (calendar + server-side RSVP)](docs/tickets/0036-graph-sync-backend.md) -- feature _(blocked by #0035)_
- [#0082 Verify the Graph delta against a live tenant](docs/tickets/0082-graph-delta-live-verification.md) -- perf _(the split-out half of #0042, which shipped the `/messages/delta` path and its fallbacks but had no Graph account to smoke them against)_
- [#0063 Send durability gaps, Graph half](docs/tickets/0063-send-durability-gaps.md) -- bug _(the SMTP halves shipped; scope item 3, resumable Graph `pending_send` rows, waits with the backend)_

The parity half of [#0059](docs/tickets/0059-syncbackend-trait.md) is parked with it: the trait and the engine shipped, but `graph.rs` still runs its own loop rather than being a second `SyncBackend`.
