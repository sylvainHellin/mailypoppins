$ mp --help
A terminal email client: Markdown drafts on disk, received mail in a local store

Usage: mp [OPTIONS] [SELECTOR] [COMMAND]

Commands:
  send            Send a single approved email, or (with --invite) a calendar invitation
  send-approved   Send every approved draft of the account
  list            List the account's drafts from the drafts index
  validate        Validate a draft's frontmatter (default: every draft of the account)
  mark-approved   Mark a draft as approved
  mark-draft      Demote an approved draft back to `draft` status (reverse of `mark-approved`)
  new             Create a new email draft from template and print its selector
  path            Print the filesystem path of a draft (the only selector-to-path edge)
  edit            Open a draft in $EDITOR
  reply           Create a reply draft from a received email
  forward         Forward an email to new recipients
  invite          RSVP to a received calendar invitation (iMIP REPLY over SMTP)
  list-mailboxes  List available IMAP mailboxes/folders
  fetch           Fetch emails from IMAP server
  sync            Sync mailboxes from the server into the local store
  watch           Watch a mailbox for changes using IMAP IDLE
  archive         Archive a received email (server + local)
  delete          Delete a received email (server + local) or a local draft
  open            Open a received email's attachment in the default application
  save            Save a received email's attachment(s) to a directory
  show            Print one received message from the local store (offline)
  list-messages   List received messages from the local store (offline)
  search          Search emails on the IMAP server, or locally with --local
  config          Manage configuration
  contacts        Contact index operations
  calendar        Calendar / iMIP invite operations
  outbox          Inspect and unblock the durable send queue
  store           Local store maintenance (retention garbage collection)
  cutover         Report what is left of the file-era `.md` tree, and import its drafts
  dump-keys       Dump the TUI key bindings from the single KEYMAP source of truth
  dump-mailbox    Dump message envelopes from the local message store as NDJSON
  help            Print this message or the help of the given subcommand(s)

Arguments:
  [SELECTOR]  Draft selector to preview (dry-run mode)

Options:
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help
  -V, --version                Print version

$ mp send --help
Send a single approved email, or (with --invite) a calendar invitation

Usage: mp send [OPTIONS] [SELECTOR]

Arguments:
  [SELECTOR]  Draft selector: mp://<account>/drafts/<id>, drafts/<id> or <id> (omit when using --invite)

Options:
  -y, --yes                        Skip confirmation prompt
      --invite                     Send an iMIP calendar invitation (METHOD:REQUEST) instead of a draft. Attendees come from --to/--cc; the subject is used as the event summary. Requires --to, --start, --subject, and one of --end/--duration
      --to <TO>                    Invite recipient(s), comma-separated (invite mode; ATTENDEE + To)
      --cc <CC>                    Invite CC recipient(s), comma-separated (invite mode; ATTENDEE + Cc)
      --subject <SUBJECT>          Event subject / summary (invite mode)
      --start <START>              Event start. Local time (2026-07-20T14:00 or "2026-07-20 14:00") or RFC3339 with offset (2026-07-20T14:00:00+02:00, ...Z). Invite mode
      --end <END>                  Event end (same formats as --start). Provide this or --duration
      --duration <DURATION>        Event duration instead of --end: ISO8601 (PT1H30M) or short (1h30m)
      --location <LOCATION>        Optional event location (invite mode)
      --description <DESCRIPTION>  Optional event description / body (invite mode)
  -s, --signature <SIGNATURE>      Signature to use (overrides config default)
      --no-signature               Skip signature entirely
  -A, --account <ACCOUNT>          Account to use (default: first in config)
  -h, --help                       Print help

$ mp send-approved --help
Send every approved draft of the account

Usage: mp send-approved [OPTIONS]

Options:
      --all-accounts           Send the approved drafts of every configured account
  -y, --yes                    Skip confirmation prompt
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp list --help
List the account's drafts from the drafts index

Usage: mp list [OPTIONS]

Options:
      --status <STATUS>        Only list drafts with this status [possible values: draft, approved, sent]
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp validate --help
Validate a draft's frontmatter (default: every draft of the account)

Usage: mp validate [OPTIONS] [SELECTOR]

Arguments:
  [SELECTOR]  Draft selector: mp://<account>/drafts/<id>, drafts/<id> or <id>

Options:
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp mark-approved --help
Mark a draft as approved

Usage: mp mark-approved [OPTIONS] <SELECTOR>

Arguments:
  <SELECTOR>  Draft selector: mp://<account>/drafts/<id>, drafts/<id> or <id>

Options:
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp mark-draft --help
Demote an approved draft back to `draft` status (reverse of `mark-approved`)

Usage: mp mark-draft [OPTIONS] <SELECTOR>

Arguments:
  <SELECTOR>  Draft selector: mp://<account>/drafts/<id>, drafts/<id> or <id>

Options:
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp new --help
Create a new email draft from template and print its selector

Usage: mp new [OPTIONS] <NAME>

Arguments:
  <NAME>  Name for the new draft file

Options:
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp path --help
Print the filesystem path of a draft (the only selector-to-path edge)

Usage: mp path [OPTIONS] <SELECTOR>

Arguments:
  <SELECTOR>  Draft selector: mp://<account>/drafts/<id>, drafts/<id> or <id>

Options:
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp edit --help
Open a draft in $EDITOR

Usage: mp edit [OPTIONS] <SELECTOR>

Arguments:
  <SELECTOR>  Draft selector: mp://<account>/drafts/<id>, drafts/<id> or <id>

Options:
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp reply --help
Create a reply draft from a received email

Usage: mp reply [OPTIONS] <SELECTOR>

Arguments:
  <SELECTOR>  Received selector: mp://<account>/<mailbox>/<message-id>, <mailbox>/<message-id> or <message-id>

Options:
      --all                    Reply to all recipients
      --mailbox <MAILBOX>      Mailbox to resolve the selector in
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp forward --help
Forward an email to new recipients

Usage: mp forward [OPTIONS] <SELECTOR>

Arguments:
  <SELECTOR>  Received selector: mp://<account>/<mailbox>/<message-id>, <mailbox>/<message-id> or <message-id>

Options:
      --mailbox <MAILBOX>      Mailbox to resolve the selector in
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp invite --help
RSVP to a received calendar invitation (iMIP REPLY over SMTP)

Usage: mp invite [OPTIONS] <COMMAND>

Commands:
  accept     Accept the invitation (PARTSTAT=ACCEPTED)
  tentative  Tentatively accept the invitation (PARTSTAT=TENTATIVE)
  decline    Decline the invitation (PARTSTAT=DECLINED)
  help       Print this message or the help of the given subcommand(s)

Options:
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp invite accept --help
Accept the invitation (PARTSTAT=ACCEPTED)

Usage: mp invite accept [OPTIONS] <SELECTOR>

Arguments:
  <SELECTOR>  Received selector of the invitation: mp://<account>/<mailbox>/<message-id>

Options:
      --mailbox <MAILBOX>      Mailbox to resolve the selector in
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp invite tentative --help
Tentatively accept the invitation (PARTSTAT=TENTATIVE)

Usage: mp invite tentative [OPTIONS] <SELECTOR>

Arguments:
  <SELECTOR>  Received selector of the invitation: mp://<account>/<mailbox>/<message-id>

Options:
      --mailbox <MAILBOX>      Mailbox to resolve the selector in
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp invite decline --help
Decline the invitation (PARTSTAT=DECLINED)

Usage: mp invite decline [OPTIONS] <SELECTOR>

Arguments:
  <SELECTOR>  Received selector of the invitation: mp://<account>/<mailbox>/<message-id>

Options:
      --mailbox <MAILBOX>      Mailbox to resolve the selector in
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp list-mailboxes --help
List available IMAP mailboxes/folders

Usage: mp list-mailboxes [OPTIONS]

Options:
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp fetch --help
Fetch emails from IMAP server

Usage: mp fetch [OPTIONS]

Options:
      --from <FROM>            Filter by sender address
      --to <TO>                Filter by recipient address
      --cc <CC>                Filter by CC address
      --subject <SUBJECT>      Subject contains
      --body <BODY>            Body contains
      --since <SINCE>          Emails since date (YYYY-MM-DD)
      --before <BEFORE>        Emails before date (YYYY-MM-DD)
  -n, --limit <LIMIT>          Max results (default: 10) [default: 10]
      --full                   Show full body instead of preview
      --mailbox <MAILBOX>      Mailbox name (default: INBOX) [default: INBOX]
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp sync --help
Sync mailboxes from the server into the local store

Usage: mp sync [OPTIONS]

Options:
  -n, --limit <LIMIT>          Max messages per mailbox (default: 50) [default: 50]
      --mailbox <MAILBOX>      Mailboxes to sync (default: INBOX, Archive, Sent)
      --dry-run                Show what would be ingested without writing anything
      --all-accounts           Sync every configured account (failures are named at the end; exit code 1 if any account failed)
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp watch --help
Watch a mailbox for changes using IMAP IDLE

Usage: mp watch [OPTIONS]

Options:
      --mailbox <MAILBOX>      Mailbox to watch (default: INBOX) [default: INBOX]
      --timeout <TIMEOUT>      Timeout in seconds (exits with code 2 on timeout)
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp archive --help
Archive a received email (server + local)

Usage: mp archive [OPTIONS] <SELECTOR>

Arguments:
  <SELECTOR>  Received selector: mp://<account>/<mailbox>/<message-id>

Options:
      --mailbox <MAILBOX>      Mailbox to resolve the selector in
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp delete --help
Delete a received email (server + local) or a local draft

Usage: mp delete [OPTIONS] [SELECTOR]

Arguments:
  [SELECTOR]  Received (mp://<acct>/<mbox>/<id>) or drafts (mp://<acct>/drafts/<id>) selector; omit with --sent

Options:
      --mailbox <MAILBOX>      Mailbox to resolve the selector in
      --force                  Delete an approved draft (a queued send) anyway
      --sent                   Clear every sent draft of the account (takes no selector)
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp open --help
Open a received email's attachment in the default application

Usage: mp open [OPTIONS] <SELECTOR>

Arguments:
  <SELECTOR>  Received selector: mp://<account>/<mailbox>/<message-id>

Options:
      --mailbox <MAILBOX>      Mailbox to resolve the selector in
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp save --help
Save a received email's attachment(s) to a directory

Usage: mp save [OPTIONS] <SELECTOR>

Arguments:
  <SELECTOR>  Received selector: mp://<account>/<mailbox>/<message-id>

Options:
  -o, --output <OUTPUT>        Output directory (default: current directory)
      --mailbox <MAILBOX>      Mailbox to resolve the selector in
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp show --help
Print one received message from the local store (offline)

Usage: mp show [OPTIONS] <SELECTOR>

Arguments:
  <SELECTOR>  Received selector: mp://<account>/<mailbox>/<message-id>, <mailbox>/<message-id> or <message-id>

Options:
      --mailbox <MAILBOX>      Mailbox to resolve the selector in
      --json                   Emit one JSON object (headers, attachments and body) instead
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp list-messages --help
List received messages from the local store (offline)

Usage: mp list-messages [OPTIONS]

Options:
      --mailbox <MAILBOX>      Mailbox to list (role, slug or sidebar label). Default: every mailbox of the account, grouped
  -n, --limit <LIMIT>          Max messages per mailbox listed (default: 20) [default: 20]
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp search --help
Search emails with one grammar that every backend speaks.

Fields (combine freely, implicit AND between them):
  from:  to:  cc:  subject:  body:  filename:   match a field
  has:attachment                                 only mail with an attachment
  before:YYYY-MM-DD  after:YYYY-MM-DD             a date range (since: aliases after:)
  in:MAILBOX                                      scope to one mailbox
  message-id:<id>                                 exact Message-ID lookup

Operators:
  "a phrase"        a quoted phrase is one term
  a OR b            either term
  (a OR b)          a parenthesised OR group, AND-ed with the rest

Examples:
  mp search 'from:boss@corp.com (invoice OR receipt) has:attachment'
  mp search --from boss@corp.com --has-attachment 'invoice OR receipt'
  mp search 'subject:"quarterly report" after:2026-01-01 before:2026-07-01'
  mp search --local 'from:ada ledger'

Backend honesty: on Gmail and Exchange every term runs server-side. On plain
IMAP has:attachment has no server key, so it is answered from the local store
(synced mail only) and the run prints a warning. filename: is Gmail/Exchange/
--local only.

Usage: mp search [OPTIONS] [QUERY]

Arguments:
  [QUERY]
          Search query. One grammar for every backend: fields from: to: cc: subject: body: filename:, the flag has:attachment, dates before:YYYY-MM-DD / after:YYYY-MM-DD (since: aliases after:), quoted "phrases", OR and (a OR b) groups, plus in: and message-id:. Combine with the flags below; they build the same query
          
          [default: ]

Options:
      --mailbox <MAILBOX>
          Mailbox to search (default: all the account's mailboxes)

      --from <FROM>
          Match the sender (from:)

      --to <TO>
          Match a recipient (to:)

      --cc <CC>
          Match a Cc recipient (cc:)

      --subject <SUBJECT>
          Match the subject (subject:)

      --body <BODY>
          Match the body (body:)

      --filename <FILENAME>
          Match an attachment filename (Gmail/Exchange/--local only)

      --has-attachment
          Only mail carrying an attachment (has:attachment)

      --after <AFTER>
          On or after this date, YYYY-MM-DD (after:)

      --before <BEFORE>
          Strictly before this date, YYYY-MM-DD (before:)

  -n, --limit <LIMIT>
          Max results (default: 20)
          
          [default: 20]

      --full
          Show full body instead of preview

      --local
          Search the local store's full-text index instead of the server (offline, ranked, covers every synced mailbox at once)

  -s, --signature <SIGNATURE>
          Signature to use (overrides config default)

      --no-signature
          Skip signature entirely

  -A, --account <ACCOUNT>
          Account to use (default: first in config)

  -h, --help
          Print help (see a summary with '-h')

$ mp config --help
Manage configuration

Usage: mp config [OPTIONS] <COMMAND>

Commands:
  init           Interactive setup wizard
  show           Show current configuration
  set-password   Store a password in the active secrets backend
  reset-secrets  Wipe the encrypted secrets file (and OAuth2 token caches) and re-prompt for credentials. Use this after a Time Machine restore to a new machine, or whenever the secrets file can no longer be decrypted
  add-account    Add a new account to the existing config
  oauth2-login   Run OAuth2 device code flow to acquire and cache a token
  path           Print config file path
  help           Print this message or the help of the given subcommand(s)

Options:
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp config init --help
Interactive setup wizard

Usage: mp config init [OPTIONS]

Options:
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp config show --help
Show current configuration

Usage: mp config show [OPTIONS]

Options:
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp config set-password --help
Store a password in the active secrets backend

Usage: mp config set-password [OPTIONS] <WHICH>

Arguments:
  <WHICH>  Which password to set: "smtp" or "imap"

Options:
      --account <ACCOUNT>      Account name (required if multiple accounts)
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -h, --help                   Print help

$ mp config reset-secrets --help
Wipe the encrypted secrets file (and OAuth2 token caches) and re-prompt for credentials. Use this after a Time Machine restore to a new machine, or whenever the secrets file can no longer be decrypted

Usage: mp config reset-secrets [OPTIONS]

Options:
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp config add-account --help
Add a new account to the existing config

Usage: mp config add-account [OPTIONS]

Options:
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp config oauth2-login --help
Run OAuth2 device code flow to acquire and cache a token

Usage: mp config oauth2-login [OPTIONS]

Options:
      --account <ACCOUNT>      Account name (default: first OAuth2 account)
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -h, --help                   Print help

$ mp config path --help
Print config file path

Usage: mp config path [OPTIONS]

Options:
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp contacts --help
Contact index operations

Usage: mp contacts [OPTIONS] <COMMAND>

Commands:
  search   Search the contact index
  rebuild  Rebuild the contact index from the local message store
  stats    Show index statistics
  help     Print this message or the help of the given subcommand(s)

Options:
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp contacts search --help
Search the contact index

Usage: mp contacts search [OPTIONS] [QUERY]

Arguments:
  [QUERY]  Query string (fuzzy-matched against name and email)

Options:
      --parsable               Emit tab-delimited `email\tname` lines (for mutt/aerc/vim integration)
  -n, --limit <LIMIT>          Max number of results [default: 20]
      --account <ACCOUNT>      Account name (default: first configured account)
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -h, --help                   Print help

$ mp contacts rebuild --help
Rebuild the contact index from the local message store

Usage: mp contacts rebuild [OPTIONS]

Options:
      --account <ACCOUNT>      Account name (default: all configured accounts)
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -h, --help                   Print help

$ mp contacts stats --help
Show index statistics

Usage: mp contacts stats [OPTIONS]

Options:
      --account <ACCOUNT>      Account name (default: first configured account)
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -h, --help                   Print help

$ mp calendar --help
Calendar / iMIP invite operations

Usage: mp calendar [OPTIONS] <COMMAND>

Commands:
  rebuild  Report what the stored attendee REPLY emails resolve on the stored invitations. Writes nothing: attendee statuses are derived from the `invite.ics` payloads wherever they are displayed, so there is no cached copy to rebuild
  help     Print this message or the help of the given subcommand(s)

Options:
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp calendar rebuild --help
Report what the stored attendee REPLY emails resolve on the stored invitations. Writes nothing: attendee statuses are derived from the `invite.ics` payloads wherever they are displayed, so there is no cached copy to rebuild

Usage: mp calendar rebuild [OPTIONS]

Options:
      --account <ACCOUNT>      Account name (default: all configured accounts)
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -h, --help                   Print help

$ mp outbox --help
Inspect and unblock the durable send queue

Usage: mp outbox [OPTIONS] <COMMAND>

Commands:
  list     List every queued, retrying, failed or partly delivered submission
  retry    Send a failed submission again (only after checking it did not arrive)
  discard  Drop a submission and release the message bytes it holds
  help     Print this message or the help of the given subcommand(s)

Options:
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp outbox list --help
List every queued, retrying, failed or partly delivered submission

Usage: mp outbox list [OPTIONS]

Options:
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp outbox retry --help
Send a failed submission again (only after checking it did not arrive)

Usage: mp outbox retry [OPTIONS] <ID>

Arguments:
  <ID>  Outbox row id, as shown by `mp outbox list`

Options:
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp outbox discard --help
Drop a submission and release the message bytes it holds

Usage: mp outbox discard [OPTIONS] <ID>

Arguments:
  <ID>  Outbox row id, as shown by `mp outbox list`

Options:
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp store --help
Local store maintenance (retention garbage collection)

Usage: mp store [OPTIONS] <COMMAND>

Commands:
  gc    Run the retention sweep now: evict cached blobs over the disk cap
  help  Print this message or the help of the given subcommand(s)

Options:
  -s, --signature <SIGNATURE>  Signature to use (overrides config default)
      --no-signature           Skip signature entirely
  -A, --account <ACCOUNT>      Account to use (default: first in config)
  -h, --help                   Print help

$ mp store gc --help
Run the retention sweep now: evict cached blobs over the disk cap.

The first over-cap run only warns and records a marker; run it again to evict. `--dry-run` prints what would go without touching anything. A run that would reclaim more than half the store's blob bytes is refused without `--force` (a fat-finger guard while on-demand re-fetch, #0085, does not yet exist).

Usage: mp store gc [OPTIONS]

Options:
      --dry-run
          Print what would be evicted and stop; change nothing

      --force
          Evict even when the plan would reclaim more than half the store

      --all-accounts
          Sweep every configured account rather than just the default / `-A`

  -s, --signature <SIGNATURE>
          Signature to use (overrides config default)

      --no-signature
          Skip signature entirely

  -A, --account <ACCOUNT>
          Account to use (default: first in config)

  -h, --help
          Print help (see a summary with '-h')

$ mp cutover --help
Report what is left of the file-era `.md` tree, and import its drafts.

Assigns an `id:` frontmatter field to any draft that has none, so a file-era draft becomes addressable by selector, then names the file-era mailbox directories nothing reads any more and prints the command that removes them. It deletes nothing itself. `--dry-run` writes not even the `id:` field.

Usage: mp cutover [OPTIONS]

Options:
      --account <ACCOUNT>
          Account name (default: all configured accounts)

      --dry-run
          Report only; write nothing at all

  -s, --signature <SIGNATURE>
          Signature to use (overrides config default)

      --no-signature
          Skip signature entirely

  -h, --help
          Print help (see a summary with '-h')

$ mp dump-keys --help
Dump the TUI key bindings from the single KEYMAP source of truth.

Markdown by default; `--json` emits the section-grouped shape the website consumes. Regenerate the site data with: `mp dump-keys --json > website/src/data/tui-keys.json` (see scripts/regen-website-keys.sh).

Usage: mp dump-keys [OPTIONS]

Options:
      --json
          Emit JSON grouped by section instead of Markdown

  -s, --signature <SIGNATURE>
          Signature to use (overrides config default)

      --no-signature
          Skip signature entirely

  -A, --account <ACCOUNT>
          Account to use (default: first in config)

  -h, --help
          Print help (see a summary with '-h')

$ mp dump-mailbox --help
Dump message envelopes from the local message store as NDJSON.

Offline: reads the local store only, never the network. One compact JSON object per line, with the fields account, mailbox, message_id, from, to, cc, subject, date_sort, flags, attachments (name + size) and invite. No filesystem paths appear in the output.

Records are sorted by account, mailbox, date_sort, message_id and subject (the message's uid breaks remaining ties without being emitted), so two runs over an unchanged store are byte-identical.

Dumps every configured account by default; `-A/--account` restricts it to one, `--mailbox` to the named mailboxes.

Usage: mp dump-mailbox [OPTIONS] --json

Options:
      --json
          Emit newline-delimited JSON. Currently the only output format, and required, so a later default cannot silently change this one

      --mailbox <MAILBOX>
          Mailbox to dump (role, slug or sidebar label; repeatable). Default: every mailbox of every selected account

  -s, --signature <SIGNATURE>
          Signature to use (overrides config default)

      --no-signature
          Skip signature entirely

  -A, --account <ACCOUNT>
          Account to use (default: first in config)

  -h, --help
          Print help (see a summary with '-h')

